kubernetes==35.0.0
hvac==2.4.0
dnspython==2.8.0
requests==2.32.5
cryptography==46.0.3       # Certificate parsing for LetsEncrypt validation

# Pytest dependencies
pytest==9.0.2
pytest-xdist==3.8.0        # Parallel execution
allure-pytest==2.15.3      # Allure reports with native screenshot attachments
pytest-json-report==1.5.0  # JSON reports for CI/CD
pytest-timeout==2.4.0      # Timeout handling
pytest-rerunfailures==16.1 # Automatic retry for flaky tests

# UI Testing
playwright==1.58.0
pyotp==2.9.0               # TOTP 2FA for GitHub login
# Note: BrowserBase uses REST API (via requests) - no SDK needed

# QR Code validation
Pillow==12.1.0             # Image processing for QR code decoding
pyzbar==0.1.9              # QR code decoding library

# GitHub API
PyGithub==2.8.1            # GitHub API client for GitOps tests

# Static analysis and code quality
mypy==1.19.1               # Static type checking
pylint==4.0.4              # Code linting
types-requests==2.32.4.20250913  # Type stubs for requests library

# Visual regression testing
pixelmatch==0.3.0          # Fast pixel-level image comparison
numpy==2.2.6               # Image array processing for pixelmatch
