# Kubernetes Configuration
# Path to kubeconfig file (defaults to ~/.kube/config if not set)
# KUBECONFIG=/path/to/kubeconfig

# Cluster Configuration
# The captain domain for your cluster (e.g., nonprod.foobar.onglueops.rocks)
CAPTAIN_DOMAIN=nonprod.foobar.onglueops.rocks

# GitHub Credentials (required for UI tests)
# GitHub username or email
GITHUB_USERNAME=your-github-username@example.com
# GitHub password
GITHUB_PASSWORD=your-github-password
# GitHub OTP secret for 2FA (base32 encoded TOTP secret)
GITHUB_OTP_SECRET=YOUR_OTP_SECRET_HERE

# BrowserBase Configuration (optional - for remote browser testing)
# Set to "True" to use BrowserBase instead of local Chrome
USE_BROWSERBASE=False
# BrowserBase API key (get from https://browserbase.com)
BROWSERBASE_API_KEY=bb_live_your_api_key_here
# BrowserBase project ID
BROWSERBASE_PROJECT_ID=your-project-id-here

# GitHub API Configuration (required for GitOps tests)
# GitHub personal access token with repo permissions (create at https://github.com/settings/tokens)
GITHUB_TOKEN=ghp_your_token_here
# Template repository URL with optional tag (tag in URL takes precedence)
# Format: https://github.com/org/repo or https://github.com/org/repo/releases/tag/0.1.0
TEMPLATE_REPO_URL=https://github.com/GlueOps/deployment-configurations/releases/tag/0.1.0
# Destination repository URL where ephemeral test repo will be created (will be deleted after tests)
# Format: https://github.com/destination-org/destination-repo
DESTINATION_REPO_URL=https://github.com/destination-org-name/destination-repo-name

# Tenant Configuration (preferred over DESTINATION_REPO_URL for new tests)
# GitHub organization name for the tenant
TENANT_GITHUB_ORGANIZATION_NAME=development-tenant-jupiter
# Deployment configurations repository name
TENANT_DEPLOYMENT_CONFIGURATIONS_REPO_NAME=deployment-configurations
# Template repository URL for deployment configurations (alternative to TEMPLATE_REPO_URL)
DEPLOYMENT_CONFIGURATIONS_TEMPLATE_REPO=https://github.com/GlueOps/deployment-configurations/releases/tag/0.1.0

# Captain Domain Repository (required for captain_manifests fixture)
# GitHub URL for the captain domain configuration repository
CAPTAIN_DOMAIN_REPO_URL=https://github.com/development-captains/nonprod.jupiter.onglueops.rocks
# GitHub token with access to the captain domain repository (separate from GITHUB_TOKEN)
CAPTAIN_DOMAIN_GITHUB_TOKEN=github_pat_xxx

# DNS services for wildcard domain resolution (comma-separated for fallback)
# These services provide automatic DNS resolution: <name>-<ip-dashed>.{service} resolves to <ip>
# Multiple services enable automatic fallback on test retry (e.g., if sslip.io is down, try nip.io)
# For single service, just specify one: WILDCARD_DNS_SERVICES=nip.io
WILDCARD_DNS_SERVICES=nip.io,sslip.io,traefik.me

# Test Debugging Configuration
# Pause before test teardown for manual inspection of resources
# Set to any truthy value (1, true, TRUE, yes, y, on) to enable interactive pause
# When enabled, tests will block and wait for Enter key before cleanup begins
# Useful for inspecting ArgoCD apps, namespaces, GitHub repos, etc. during test development
# Usage: TEARDOWN_WAIT=1 pytest tests/test_deployment_workflow.py
# TEARDOWN_WAIT=1
