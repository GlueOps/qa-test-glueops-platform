# Kubernetes Configuration
# Path to kubeconfig file (defaults to ~/.kube/config if not set)
# KUBECONFIG=/path/to/kubeconfig

# Cluster Configuration
# The captain domain for your cluster (e.g., nonprod.foobar.onglueops.rocks)
CAPTAIN_DOMAIN=nonprod.foobar.onglueops.rocks

# GitHub Credentials (required for UI tests)
# GitHub username or email
GITHUB_USERNAME=your-github-username@example.com
# GitHub password
GITHUB_PASSWORD=your-github-password
# GitHub OTP secret for 2FA (base32 encoded TOTP secret)
GITHUB_OTP_SECRET=YOUR_OTP_SECRET_HERE

# BrowserBase Configuration (optional - for remote browser testing)
# Set to "True" to use BrowserBase instead of local Chrome
USE_BROWSERBASE=False
# BrowserBase API key (get from https://browserbase.com)
BROWSERBASE_API_KEY=bb_live_your_api_key_here
# BrowserBase project ID
BROWSERBASE_PROJECT_ID=your-project-id-here

# GitHub API Configuration (required for GitOps tests)
# GitHub personal access token with repo permissions (create at https://github.com/settings/tokens)
GITHUB_TOKEN=ghp_your_token_here
# Template repository URL with optional tag (tag in URL takes precedence)
# Format: https://github.com/org/repo or https://github.com/org/repo/releases/tag/0.1.0
TEMPLATE_REPO_URL=https://github.com/GlueOps/deployment-configurations/releases/tag/0.1.0
# Destination repository URL where ephemeral test repo will be created (will be deleted after tests)
# Format: https://github.com/destination-org/destination-repo
DESTINATION_REPO_URL=https://github.com/destination-org-name/destination-repo-name

# DNS service for wildcard domain resolution
# sslip.io provides automatic DNS resolution: <name>-<ip-dashed>.sslip.io resolves to <ip>
# Can be changed to alternatives like nip.io or a custom domain
WILDCARD_DNS_SERVICE=sslip.io
